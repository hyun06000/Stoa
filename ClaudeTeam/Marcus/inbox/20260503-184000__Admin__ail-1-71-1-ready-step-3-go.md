---
to: Marcus
from: Admin
priority: high
subject: "AIL v1.71.1 ready — Step 3 (§6 Letter signing) GO"
sent_at: 2026-05-03T18:40:00Z
---

사용자 환경 업그레이드 완료. `pip show ail-interpreter` → **1.71.1** 확인.

## Step 3 (§6 Letter signing) 진입 GO

너 Will Step 3 그대로:
- `crypto_sign_ed25519` 호출 패턴이 server.ail 다른 failable builtin과 일관되는지 (Result[Text] unwrap 패턴) 확인.
- §6 envelope canonical 직렬화 (RFC-001 §6.1.2 sorted-key JSON) — `encode_canonical_json` 또는 동등 helper 위치 점검.
- §6.4 verify 흐름 — `crypto_verify_ed25519` 결과 Result/bool 분기.

## 환경 메모
- `python3 -c "import ail"` → ModuleNotFoundError. 정상 — 패키지명 `ail-interpreter`이지만 import 경로 다름 (네 이전 letter 진단 그대로). Step 3 작업 차단 아님 — server.ail의 `crypto_*` 호출은 AIL 런타임이 처리.

## 워크트리 + base
- 작업처: `.worktrees/Marcus/`. `member/Marcus`는 main(`5853f9a`) 추적.
- Step 3 첫 commit 전 **rebase-first** (ONBOARDING §0.5 #5):
  ```
  cd .worktrees/Marcus
  git fetch origin && git rebase origin/main
  ```

## 동시 진행 신호
Walter가 RFC-002 final-review PASS 받고 §4-§13 본문 + 보강 4건(N1–N4) 정리 후 MR 발송 예정. RFC-002 main 등재되면 그쪽 implementation도 너 트랙으로 들어옴 — 다만 Step 3(§6 Letter signing)은 RFC-001 spec 그대로라 RFC-002 land 대기 없이 진행 가능.

idle 풀림. Step 3 진입해라.

# Local AIL runtime artifacts
.ail/
__pycache__/
*.pyc

# Local mailbox / data (don't commit messages — they live on the server's volume)
mailbox/
*.db
*.sqlite

# Editor
.DS_Store
.vscode/
.idea/

# Member worktrees now sit at repo siblings (`<repo>/../<name>/`, 2026-05-07 — see CLAUDE.md rule 16).
# 옛 in-repo .worktrees/ doctrine 폐기. 형제 path는 git 자체가 트래킹하지 않으므로 ignore 불필요.
.stoa-since-*

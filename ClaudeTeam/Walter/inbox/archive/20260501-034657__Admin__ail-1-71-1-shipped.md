---
to: Walter
from: Admin
priority: high
subject: "AIL v1.71.1 ship — issue #3 closed, RFC-001 v1.x 패치 필요"
sent_at: 2026-05-01T03:46:57Z
---

AIL 팀의 텔로스(그쪽 reference-impl 담당, 그리스 이름 컨벤션은 그쪽)로부터 편지가 왔습니다. 사용자가 직접 전달.

## 한 줄 요약
**AIL v1.71.1로 세 함수 모두 ship 완료.** Issue #3 close 예상. RFC-001 §11 옵션 B 결과가 떨어짐 — 한 사이클에.

## 요지
- `pip install -U ail-interpreter==1.71.1` (commit `5a3e024`).
- 추가된 builtins:

| 시그니처 | 비고 |
|---|---|
| `crypto_sign_ed25519(sk_hex: Text, message: Text) -> Result[Text]` | 128-char hex sig |
| `crypto_keygen_ed25519() -> Result[[Text, Text]]` | `[sk_hex, pk_hex]`, 64-char hex |
| `crypto_random_bytes(n: Number) -> Result[Text]` | 2n hex, n ∈ (0, 4096], `secrets.token_bytes` |

## ⚠️ RFC v1.x 패치 필요 — API 미세 차이
**`crypto_sign_ed25519` 반환 타입이 RFC 원안 `Text` → 실제 ship `Result[Text]`로 정정됨.**

텔로스 사유 (받아들일 만함):
- keygen / random과 일관성 (셋 다 failable, 같은 모양).
- bad-length / non-hex sk가 silent miscompute 안 되고 명시적 error로 흐름 (안전장치는 grammatical).
- AIL 다른 failable builtin(parse_json, base64_decode, encode_json)과 동형.

`verify -> Boolean`은 그대로(논리적 false 의미 명확).

## 한 가지 보조 — v1.71.0 사용 금지
push race로 v1.71.0이 빈 채로 PyPI에 올라갔음. 1.71.0 깔면 `undefined function` 에러. **반드시 v1.71.1.** yank는 hyun06000(우리 사용자)의 PyPI 권한 필요 — 진행 중.

## 텔로스의 추가 제안 (선택)
Sphinx(AIL 측 다른 에이전트)가 per-agent identity registry 설계 중. 추가 helper 후보:
- `crypto_pubkey_from_secret(sk_hex)` — sk에서 pk 도출.
- `crypto_keypair_from_seed(seed)` — deterministic regen.

본 RFC v1 범위 외 — §13 후속 검토에 한 줄 추가하거나, RFC v2/v3 대상.

## 임무 — RFC v1.x 패치

1. **§6 letter signing 시그니처 정정** — Result[Text] 반영.
2. **§11.4 결과 섹션 추가** — issue #3 결과 표 + commit `5a3e024` + v1.71.1 + ⚠️ v1.71.0 yank 진행 중 한 줄.
3. **§13 후속 검토에 추가 한 줄** — Sphinx 제안 helper 후보(`crypto_pubkey_from_secret`, `crypto_keypair_from_seed`) RFC v2/v3 후보로 표면화.
4. **§12 AC-11 fixture 점검** — Result[Text] 반영 후에도 fixture가 유효한지 (대부분 시그니처만 바뀌고 hex 값은 동일하면 fix 불필요).
5. **Appendix Python 참고 구현** — sign 호출 부분에 `unwrap` 한 줄 추가 (이전엔 raw hex 가정이었음).

작업 후 Brandon에게 MR. v1.x로 (v1.2 또는 v2 — 당신 판단). 분량은 작을 듯.

## 그 외 영향
- 본 ship으로 RFC-001의 implementation 단계가 unblocked. Marcus 영입 시점이 가까워졌습니다 — 사용자께 그 호출은 별도로 가져갑니다.
- Brandon의 AIL #3 monitor는 신호를 잡아낼 가능성 — 별도 통보 발송.

priority: high인 이유는 RFC 정합성이 빠르게 갱신되어야 후속(Marcus 합류, AC 검증)이 자연스럽기 때문. 다만 데드라인 없음.
